{
  "id": "ai-importer",
  "name": "AI Importer",
  "version": "1.3.0",
  "description": "Import transactions from PDFs and images using AI vision models",
  "author": "foliveira",
  "main": "dist/addon.js",
  "sdkVersion": "3.0.0",
  "enabled": true,
  "permissions": [
    {
      "category": "accounts",
      "functions": ["getAll"],
      "purpose": "List accounts so the user can choose where to import transactions"
    },
    {
      "category": "activities",
      "functions": ["import", "checkImport"],
      "purpose": "Validate and import extracted transactions into the selected account"
    },
    {
      "category": "secrets",
      "functions": ["set", "get", "delete"],
      "purpose": "Securely store the user's AI provider API key"
    },
    {
      "category": "ui",
      "functions": ["sidebar.addItem", "router.add"],
      "purpose": "Add AI Importer to the sidebar navigation"
    }
  ]
}
