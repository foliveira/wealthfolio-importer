{
  "name": "wealthfolio-ai-importer-addon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "dev:server": "vite --port 3001",
    "build": "tsc && vite build",
    "bundle": "npm run build && cp manifest.json dist/ && cd dist && zip -r ../ai-importer.zip ."
  },
  "dependencies": {
    "pdfjs-dist": "^5.6.205",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
