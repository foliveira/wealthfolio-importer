{
  "name": "wealthfolio-importer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix addon",
    "dev:server": "npm run dev:server --prefix addon",
    "build": "npm run build --prefix addon",
    "bundle": "npm run bundle --prefix addon"
  }
}
